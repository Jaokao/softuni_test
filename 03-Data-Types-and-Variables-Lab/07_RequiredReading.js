function solve(numberPages, pagesPerHour, daysToRead) {

    let totalTimeToRead = numberPages / pagesPerHour;
    let reqHoursPerDay = totalTimeToRead / daysToRead;

    console.log(reqHoursPerDay)
}
solve(212,
    20 ,
    2 
    )
solve(432,
    15 ,
    4 
    )